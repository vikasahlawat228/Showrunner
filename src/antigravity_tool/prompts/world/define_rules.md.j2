{# Template: Define World Rules #}
{# Step: world_building #}
{# access_level: story #}

# Task: Define World Rule - {{ new_rule_name }}

Category: **{{ new_rule_category }}**

{% if existing_world %}
## World Context

- **World**: {{ existing_world.name }} ({{ existing_world.genre }})
- **Technology Level**: {{ existing_world.technology_level }}

{% if existing_world.rules %}
### Existing Rules
{% for rule in existing_world.rules %}
- **{{ rule.name }}** ({{ rule.category }}): {{ rule.description }}
{% endfor %}
{% endif %}
{% endif %}

## Instructions

Define the rule **{{ new_rule_name }}** for this world. Ensure it is:
1. Internally consistent with existing rules
2. Has clear limitations (every power/system needs costs)
3. Has visual manifestations (how does it LOOK when used in panels?)

## Output Format

```yaml
name: "{{ new_rule_name }}"
category: "{{ new_rule_category }}"
description: |
  Detailed description of how this rule works.
  Include visual manifestations for panel art.
limitations:
  - "Cost or constraint 1"
  - "Cost or constraint 2"
known_to_reader: true  # Set false if this is a hidden mechanic
```
